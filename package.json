{
  "name": "vulpo-app",
  "version": "1.0.0",
  "private": true,
  "description": "Empaque Android (Capacitor) de VULPO. Solo la app; el sitio web se sigue sirviendo tal cual desde la raiz.",
  "scripts": {
    "webdir": "python scripts/armar-webdir.py"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0"
  }
}
